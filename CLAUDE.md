# MLOps-Edge-Lab

개인 학습 프로젝트 — 1) MLOps 파이프라인 구축, 2) 엣지 디바이스용 sLLM 구축. 완료 후 교육 자료로 정리할 계획.

## 작업 원칙 (가장 중요)

**이 프로젝트는 학습이 목적이다.** 결과물만 던지지 말고, 진행하는 각 단계마다 "왜 이렇게 하는지"를 설명할 것 — 다른 선택지가 있었다면 무엇이었고 왜 이걸 골랐는지까지 포함한다. 정답만 아는 것과 이해하고 아는 것은 다르다. 이 과정 자체가 나중에 교육 자료의 재료가 된다.

## 배경 문서

- `docs/전략.md` — 서비스 아이디어, 시스템 구성도, 단계별 로드맵. **개발의 진입점**
- `docs/의사결정_로그.md` — 왜 이렇게 했는가. **결정할 때마다 항목을 추가한다**
- `docs/모델_비교.md` — sLLM 베이스 모델 후보 비교(라이선스·한국어 품질·크기), Qwen3 채택 근거

## 인프라

- AI 서버(개발+운영 겸용, GPU 4장): SSH 별칭 `grib-ai-server`로 접속. **ARWS·BidRadar가 쓰는 서버와는 완전히 별개**이니 혼동하지 말 것.
- 작업 공간은 서버의 홈 디렉토리를 사용한다(`/data`는 사용하지 않기로 결정).
- 로컬 PC는 개발에 사용하지 않는다 — 모든 작업은 AI 서버에서 수행한다.

## 하지 말 것

- ARWS·BidRadar의 코드·인프라와 섞지 않는다 (완전히 별개 프로젝트)
- 서버 접속 정보(호스트명·포트·계정)를 이 저장소의 파일에 평문으로 적지 않는다 — SSH config 별칭(`grib-ai-server`)만 참조한다
